# PATTERN-EDGE-LOG

Running log for Chart bot pattern observations. **Not a backtest. Not a track record. Not silent weights.**

Append a row when a [retro](./RETRO.template.md) is filed. Do not invent edges. Viral PnL is **never** a training label.

Columns: date (as-of) · `pattern_id` · what was claimed · what printed · invalidation hit? · `primary_force` · retro path · notes.

`primary_force` must be one of: `debt_cycle` | `internal_order` | `geopolitics` | `nature` | `inventiveness` | `DARK`.

| asof | pattern_id | claimed | printed | invalidation_hit | primary_force | retro | notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| *(no rows yet — do not fabricate)* | | | | | | | |

Empty on purpose. First desk Chart pack is **sketch** only ([docs/STATUS.md](../docs/STATUS.md)). A sketch pack does not mint log rows.
