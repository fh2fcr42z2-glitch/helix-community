# Helix Community

Public-safe agent memory. **No secrets.** Live desk truth is [docs/STATUS.md](docs/STATUS.md). Continual-learning updates only the two sections below.

## Learned User Preferences

- Public docs PRs target helix-community `main`. Do not copy helix-cx source. No secrets; env names only if any.
- A new desk surface needs a public contract markdown, a STATUS.md row, and a README link.
- RSS / public APIs first. Official X API, Google CSE, World Monitor, LunarCrush, Santiment, and CryptoPanic stay DARK without a private key.
- Nitter or HTML scrape is not a Scout PASS. Never invent tweets, follower counts, or verified badges.
- Social heat is not a Chart-shot clip and not MM size.
- Trench hull UI must match main Helix desk chrome.
- Helix does not ship archive.ph adapters (engine-only preference).
- Fetch `origin/main` before branching; Cloud Agent snapshots can lag the remote.
- Paper-only language. Viral PnL is never Helix evidence or a training label.
- Agent memory is plain bullets in this file. No evidence tags, process dumps, or keys.

## Learned Workspace Facts

- This repo is public docs. The working paper desk lives on private helix-cx PR #1 until Call Me X merges it.
- Unknown → DARK. Scout classes are FREE / FREE-TIER / PAID / DARK.
- Trench Narrative contract: [docs/TRENCH-NARRATIVE.md](docs/TRENCH-NARRATIVE.md). Reader is upstream of Alpha Writer: [docs/READER.md](docs/READER.md).
- Market Maker is LIVE-authorized on `fee_income` only; principal locked; SOL primary · ETH try-now · HYPE DARK until adapter.
- Chart shots: guest candles+volume = `sketch`; sidecar required; `helix-cowork/` may be unmounted. Do not invent PNGs.
- Chart bot learning is file-based under `ta-learning/` (empty PATTERN-EDGE-LOG until a real retro). Not silent weights.
- World Monitor and X auto-post stay DARK / off without the matching private key (read ≠ publish).
- Wallet login is SIWS/SIWE view-only. Helix never holds keys.
- Grok republish of Reader packs is not before 2026-09-14; STATUS on `main` is live now.
- Continual-learning skill + `agents-memory-updater` own this file. Teaching loop: [docs/LEARNING-PATH.md](docs/LEARNING-PATH.md) and `teaching/` (file sessions). Cadence state is gitignored under `.cursor/hooks/state/`.
